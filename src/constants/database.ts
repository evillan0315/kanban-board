export enum DatabaseKey {
  board = 'board',
  boards = 'boards',
  columns = 'columns',
  items = 'tasks',
  itemIds = 'taskIds',
  likes = 'likes',
  list = 'list',
  lists = 'lists',
  rows = 'rows',
  users = 'users',
}
